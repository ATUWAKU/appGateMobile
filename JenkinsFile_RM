pipeline {
    agent any
	triggers {
		upstream 'appGateMobile_CI, '
	}

    stages {
        stage('Ejecucion') {
            steps {				                
				bat 'gradlew clean build test aggregate'
            }			
        }
    }
}
